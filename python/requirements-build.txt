# Windows release-only dependencies. Keep hooks pinned with PyInstaller so a
# fresh release machine resolves the same packaging toolchain.
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
websocket-client==1.9.0
