# Product Atelier 下一阶段开发总交接（执行版）

> 更新日期：2026-08-21  
> 正式源码：`D:\ProductAtelier-Desktop`  
> GitHub：`https://github.com/cat644142986-afk/-.git`  
> 当前可信基线提交：`9b51da4e95339896726f1895c21ca0b816a5f68e`  
> 回滚标签：`baseline-2026-08-21-pre-workspace-refactor`  
> 本文用途：供下一位 Codex 直接接管开发；它补充并修正 `HANDOFF-2026-08-21.md` 的“下一阶段”部分。

---

## 0. 交接结论

Product Atelier 已经拥有一套可运行的 Growth Studio UI、四个工作流入口、本地创作账本、知识编译器和 Design DNA 第一版；但它还没有达到“可持续工作、可恢复、可成长”的产品标准。

下一步不得继续堆 UI 或增加模型按钮，必须先完成两项底座：

1. **持久素材工作区**：用户导入的源图必须真正落盘并可跨模式、刷新和重启恢复。
2. **可靠并发任务系统**：批量任务必须有持久队列、逐项进度、并发上限、取消、重试、部分成功和崩溃恢复。

只有这两项通过验收，才进入终稿回传、Design DNA 第二阶段、模型质量路由和 UI 精修。

---

## 1. 新 Codex 接管后的第一组操作

### 1.1 必须先读

按顺序阅读：

1. `HANDOFF-NEXT-DEVELOPMENT-2026-08-21.md`（本文）
2. `HANDOFF-2026-08-21.md`（已完成基线与产品决策）
3. `ROLLBACK.md`（安全恢复）
4. `python/atelier_ledger.py`
5. `python/server.py`
6. `src/js/app.js`
7. `src/js/api.js`

个人知识库中的项目真相页：

`C:\Users\64414\Documents\知识库\20 知识库\项目\PRJ-0001-ProductAtelier-AI商拍工作台.md`

### 1.2 先验证基线

```powershell
cd D:\ProductAtelier-Desktop
git status --short --branch
git log -3 --oneline --decorate
git tag --list "baseline-*"
npm run build
python -m py_compile python\server.py python\atelier_ledger.py python\knowledge_engine.py python\memory_engine.py
python tools\test_memory_smoke.py
cargo check --manifest-path src-tauri\Cargo.toml
```

预期：

- 分支与远端没有意外改动。
- `main` 至少包含基线提交 `9b51da4`。
- Vite、Python 编译、记忆 smoke test、Rust `cargo check` 全部通过。
- 不调用任何真实云端生图接口。

### 1.3 开发分支

不要直接在回滚标签上开发，也不要覆盖主分支历史：

```powershell
git switch main
git pull --ff-only origin main
git switch -c feature/persistent-workspace-job-engine
```

---

## 2. 产品目标与不可妥协原则

### 2.1 产品目标

Product Atelier 不是普通批量生图工具。它应当做到：

- 素材只导入一次，可以在不同工作流间重复使用。
- 多文件任务后台并发运行，用户可以同时继续组织素材、发起其他任务或查看历史。
- 每个文件、每次 Prompt、每个模型、每个输出和每条反馈都有可追溯血缘。
- 知识库在生成前提供约束，生成后参与评审，终稿回传后形成学习证据。
- 同一模型和同一素材因不同用户的 Design DNA 得到不同结果。
- UI 高级、克制、清晰，但不允许用视觉包装掩盖任务状态和数据可靠性问题。

### 2.2 禁止事项

- 禁止继续新增 `vXX-fixes.css/js`、临时补丁脚本或 MutationObserver 强制布局。
- 禁止新建第二套 SQLite 数据库；所有正式状态仍进入 `atelier.sqlite3`。
- 禁止把源素材放到 `OUTPUT_DIR/sources`；输入素材与生成结果必须分开管理。
- 禁止继续使用“一次请求启动一个 daemon 线程”的任务架构。
- 禁止把内存 `ProgressTracker` 当作权威任务状态。
- 禁止切换工作流时清空素材工作区。
- 禁止未经用户审核自动修改正式知识页。
- 禁止把 API Key、本机配置、数据库、生成图片或客户素材提交到 Git。
- 禁止未记录模型、输入、费用和输出就运行真实云端测试。
- WorkBuddy 不得独立决定数据库、并发或恢复架构；它适合做只读审计、测试清单和文档整理。

---

## 3. 当前已经完成的能力

### 3.1 UI 与桌面壳

- 暖米色 Bento Studio 主界面已完成。
- 左侧浮动导航、中央画布、右侧一屏任务控制、历史、对比、成长中心和设置页已存在。
- Logo 已移除贴边、停靠和迷你工具栏功能，只保留品牌语义。
- 红黄绿窗口按钮已接入关闭、最小化、最大化/还原。
- Windows 系统圆角和不透明 WebView 已解决边缘透出底层窗口的问题。
- `stable-ui.css` 是唯一生产样式入口；旧 UI 文件不得重新接入。

### 3.2 当前四种工作流入口

- `single`：单产品。
- `multi-file`：多张独立图片。
- `group-split`：一张合照拆分多个主体。
- `cutout-batch`：多张本地抠图。

接口存在，但“接口存在”不等于“可靠任务系统已完成”。

### 3.3 知识与成长能力

- SQLite schema v1 已记录 sessions、assets、generations、events、feedback、memory_suggestions。
- 知识编译器可以返回 Creative Brief、Intent Locks、正负规则、来源和冲突。
- 反馈可以进入本地账本。
- Design DNA 第一版能按跨会话重复证据生成待审核建议，并处理反例和去重。
- 成长中心可以接受或拒绝建议，但尚未安全写回正式知识页。

### 3.4 当前基线验证

2026-08-21 再次执行：

- `npm run build`：通过。
- Python 四个核心模块编译：通过。
- `tools/test_memory_smoke.py`：通过。
- Rust `cargo check`：通过。
- 未消耗云端额度。

---

## 4. 当前没有完成的关键问题

| 问题 | 当前事实 | 用户影响 | 优先级 |
|---|---|---|---|
| 源图未持久化 | `assets` 只保存哈希，源图 `path` 为空 | 刷新、切模式、重启后素材消失 | P0 |
| 切模式清空文件 | `switchMode()` 会调用 `clearSession()` | 多文件工作被打断，需重复上传 | P0 |
| 批量仍为串行 | `run_multi_file_task`、`run_cutout_batch_task` 使用同步 `for` 循环 | 多文件只能排队等待，效率低 | P0 |
| 无持久任务队列 | 每个入口直接创建 daemon 线程 | 无公平调度、并发上限、暂停、可靠取消 | P0 |
| 进度只在内存 | `ProgressTracker` 是内存字典 | 重启后进度消失，会话卡在 processing | P0 |
| 父子任务模型不足 | generation 无完整批次/逐项状态模型 | 无法准确表达部分成功、逐项重试 | P0 |
| batch-folder 不记账 | 文件夹批处理绕过账本 | 结果无血缘，历史不可追踪 | P1；应合并或退役 |
| 历史数据存在旧入口 | `/api/history` 仍扫描输出目录 | 与账本可能漂移 | P1 |
| 缩略图路径校验不严谨 | 使用字符串 `startswith` | 路径安全和素材根目录扩展有风险 | P1 |
| 终稿回传缺失 | 只有采用/拒绝/文字反馈 | 无法学习“生成稿到最终交付稿”的真实差异 | P1 |
| Design DNA 解析粗 | 主要依赖关键词和文本归类 | 难形成稳定、可解释的设计规则 | P1 |
| 作用域不完整 | 主要覆盖 designer/brand/category | 项目级偏好和品牌隔离不足 | P1 |
| 模型质量未基准化 | 当前模型和抠图效果没有正式回归集 | 无法证明换模型是否更好 | P2 |
| UI 尚未最终精修 | 文案偏多、左轨和参考图仍有差距、主题 DIY 未完成 | 可用但未达到设计师产品标准 | P2 |
| 自动化测试不足 | 主要是手工/脚本 smoke test | 修改队列和数据库容易回归 | P0-P3 全程补齐 |

---

## 5. 总体实施顺序

```text
数据库迁移契约
  → 素材内容寻址存储
    → 持久任务/逐项状态模型
      → 并发调度与崩溃恢复
        → 工作台和实时任务 UI
          → 终稿回传与 Design DNA
            → 模型/抠图质量路由
              → UI 精修、主题 DIY、正式发布
```

任何阶段未通过门禁，不得绕过它进入下一阶段。

---

## 6. 阶段 A：数据库迁移与数据契约（P0）

### 目标

让 schema v1 可以无损升级，为素材持久化和任务系统提供唯一权威数据模型。

### 实现方法

1. 把 `atelier_ledger.py` 当前“直接写入 schema_version”的做法改为显式迁移：
   - 读取当前版本。
   - 按 `v1 → v2 → ...` 顺序在事务中执行。
   - 迁移失败整体回滚。
   - 数据库版本高于程序支持版本时拒绝启动并输出明确错误。
2. 升级前创建同目录数据库备份，文件名包含时间和原 schema 版本。
3. 建议新增以下核心结构，最终字段可在实现前冻结：

#### `asset_blobs`

管理实际文件，不绑定某一次会话：

- `id`
- `sha256 UNIQUE`
- `storage_path UNIQUE`
- `mime`
- `size_bytes`
- `width`
- `height`
- `created_at`

现有 `assets` 继续表达“这个文件在某次会话里的角色”，新增 `blob_id` 指向 `asset_blobs`。不要立即引入收藏、标签、合集等 UI 概念。

#### `jobs`

表达一次用户提交的完整工作：

- `id`
- `session_id`
- `mode`
- `status`
- `priority`
- `total_items`
- `completed_items`
- `failed_items`
- `canceled_items`
- `requested_concurrency`
- `created_at / queued_at / started_at / updated_at / completed_at`

#### `job_items`

表达每一个文件或输出单元：

- `id`
- `job_id`
- `position`
- `source_asset_id`
- `generation_id`
- `engine_key`
- `status`
- `progress`
- `attempt_count / max_attempts`
- `error_code / error_message`
- `queued_at / started_at / updated_at / completed_at`

如果需要保留每次执行的详细历史，再增加 `task_attempts`，不要把重试覆盖在同一条不可追溯记录上。

4. 明确状态机：

```text
queued → running → completed
queued → canceled
running → canceling → canceled
running → failed → queued（人工或策略重试）
running → interrupted → queued/failed（启动恢复策略）
```

父任务状态由子项聚合得到：`queued / running / completed / partial / failed / canceled`。

### 验收标准

- [ ] 使用临时 v1 数据库升级到 v2，原 sessions/assets/generations/events/feedback/memory_suggestions 数量和内容不变。
- [ ] 同一数据库连续初始化两次不会重复迁移或报错。
- [ ] 人为制造迁移异常后，schema 和原数据保持迁移前状态。
- [ ] 遇到未来版本数据库时程序拒绝启动，不覆盖版本号。
- [ ] 所有数据库测试只使用临时目录，不接触用户正式 `%APPDATA%` 数据。
- [ ] schema 和状态机有自动化测试与文档。
- [ ] 迁移前备份可以成功打开并查询。

---

## 7. 阶段 B：持久素材工作区（P0）

### 目标

导入一次的图片在切换模式、刷新前端、重启后端和重启桌面应用后仍然存在，并可以被多个任务复用。

### 实现方法

1. 独立素材根目录：

```text
%APPDATA%\ProductAtelier\assets\<sha256前2位>\<完整sha256>.<规范扩展名>
```

`output` 只保存生成结果；`assets` 只保存导入素材。两者保留策略不同。

2. 导入流程：
   - 校验文件大小、像素、真实 MIME、可解码性。
   - 流式计算 SHA-256。
   - 写入同目录临时文件。
   - 完整写入并校验后使用原子重命名。
   - 相同哈希复用已有 blob，不重复存储。
   - 数据库提交失败时清理临时文件。
3. 增加素材 API，至少包含：导入、批量导入、读取元数据、缩略图、按 ID 获取安全文件引用。
4. 所有任务只接收 `source_asset_id`，不要再长期依赖请求内存里的 `UploadFile` 字节。
5. 缩略图服务使用 `Path.resolve()` 与 `is_relative_to()` 或等价安全判断；明确允许 `assets` 和 `output` 两个白名单根目录。
6. 前端素材状态独立于工作流状态。`single`、`multi-file`、`group-split`、`cutout-batch` 只保存各自的“当前选择集合”，不能删除公共素材。
7. 第一阶段不做自动删除。后续清理必须先有引用计数、回收站或明确用户确认。

### 验收标准

- [ ] 连续导入 20 张真实图片，数据库和磁盘都存在对应记录。
- [ ] 相同文件重复导入 3 次，物理文件只保存 1 份。
- [ ] 切换四种工作流、刷新前端、重启 Python、重启桌面应用后素材仍可见。
- [ ] 所有新建 source asset 的可访问路径不为空。
- [ ] 任务可仅凭 `source_asset_id` 在重启后重新读取源图。
- [ ] 非图片、损坏图片、超限图片返回结构化错误，不产生孤立 DB 行或临时文件。
- [ ] 模拟写盘失败和 DB 失败后没有半文件。
- [ ] 越权路径、目录穿越和白名单外路径返回 403。
- [ ] API Key、配置文件和运行数据库没有进入 Git。

---

## 8. 阶段 C：持久并发任务系统（P0）

### 目标

让多文件真正并发、可观察、可限制、可取消、可重试，并在应用崩溃或重启后恢复到一致状态。

### 实现方法

1. 用正式调度器替代入口中的 daemon 线程：
   - API 只负责创建 `job/job_items` 并入队。
   - worker 从数据库或受锁队列领取工作。
   - 任务状态和进度先写数据库，再广播给前端。
2. 使用有界 worker 池，不允许无限创建线程。
3. 按引擎设置并发闸门：
   - 云端生成：受 API 速率、费用和模型限制约束。
   - 本地抠图：受显存、内存和 ONNX Session 稳定性约束。
   - VLM 与生图引擎可以拥有不同 semaphore。
4. 公平调度：不能让一个 24 文件批次长期饿死后提交的小任务。可采用轮转或每个 job 每轮领取一个 item。
5. 取消使用协作式取消 token：
   - queued 项立即取消。
   - running 项在安全检查点停止，不留下半成品。
   - 无法中断的第三方请求进入 `canceling`，返回后丢弃结果或按策略记录。
6. 重试只针对失败项；保存 attempt、错误码、模型和耗时。
7. 启动恢复：
   - `queued` 保留并重新调度。
   - 上次进程遗留的 `running` 改为 `interrupted`。
   - 根据 attempt 次数决定重新入队或转 failed。
   - 绝不能把全部 queued 一律标记失败。
8. requests Session 改为线程本地或明确验证可并发实现；rembg/ONNX Session 必须经过并发压力测试，必要时加锁或建立小型 session 池。
9. `/api/progress/{id}` 改为读取权威持久状态；可增加 SSE/WebSocket，但轮询仍应能作为可靠回退。
10. `batch-folder` 要么迁移到同一 job 系统，要么正式退役，禁止继续成为账本外的第五套逻辑。

### 验收标准

- [ ] 20 个 mocked job items 实测同时存在至少 2 个 running，而不是串行循环。
- [ ] 每个引擎的实测峰值并发不超过配置上限。
- [ ] 两个不同 job 交错推进，后提交的小任务不会一直等待大批次全部结束。
- [ ] 可取消 queued 项和 running 项，最终状态与产物一致。
- [ ] 单项失败不会让整个批次丢失；父任务正确显示 partial。
- [ ] “重试失败项”只重跑失败项，已成功项不重复计费、不重复生成。
- [ ] 后端在运行中被终止并重启：queued 恢复，running 变 interrupted 后按策略恢复，最终没有永久 processing 会话。
- [ ] 同一提交请求重复发送不会创建无法识别的重复任务；至少支持客户端幂等键。
- [ ] 所有状态迁移都有自动化测试，非法迁移被拒绝并记录。
- [ ] 任务 ID 使用 UUID/ULID 等可靠方案，不再只依赖毫秒时间戳。
- [ ] 测试不联网、不消耗云端额度。

---

## 9. 阶段 D：素材工作台与实时任务 UI（P0/P1）

### 目标

让用户可以持续组织素材、切换工具并同时运行多个任务；任何操作都不会使已导入素材或运行状态消失。

### 实现方法

1. 统一“素材工作台”，模式只是不同处理动作：
   - 单产品：从素材区选 1 张作为当前任务输入。
   - 多文件：从素材区选择多张独立图片，支持每图方案数。
   - 合照拆分：选择 1 张合照，输出多个主体。
   - 批量抠图：选择多张素材，输出透明 PNG。
2. 四个模式保留各自选择集合和参数快照；切换模式不调用会破坏公共素材的 `clearSession()`。
3. 增加常驻任务 Dock/任务中心：
   - 全局总体进度。
   - 每个 job 的状态和成功/失败数量。
   - 每个 item 的缩略图、进度、错误和重试。
   - 暂停、继续、取消、重试失败项、打开结果、部分导出。
4. 用户可以在任务运行时切换页面、准备下一任务、查看历史或发起本地抠图。
5. 任务 UI 从后端恢复，不以当前 JS 内存状态为真相。
6. 右侧主任务控制区仍保持单屏无滚动；大批次详情进入任务 Dock、独立页面或抽屉，不把长列表硬塞入右侧面板。
7. 给所有关键按钮完整状态：idle、hover、pressed、focus、loading、disabled、success、error；动画遵守 `prefers-reduced-motion`。

### 验收标准

- [ ] 多文件导入 12 张后切换到抠图、单产品、历史再返回，12 张仍存在且顺序不变。
- [ ] 一个云端 mocked 批次运行时，可以同时发起本地 mocked 抠图任务并继续组织新素材。
- [ ] 刷新前端或关闭后重新打开，任务列表、素材、进度和结果从后端恢复。
- [ ] 总体进度、逐项进度、成功/失败/取消计数与数据库完全一致。
- [ ] 失败项可以单独重试，成功项可以先导出。
- [ ] 1280×800 下右侧任务控制区没有滚动条、遮挡或裁切。
- [ ] 960×600 下核心上传、开始、进度、取消和错误恢复仍可操作。
- [ ] 100%、125%、150%、175% DPI 下无重叠、文字截断和不可点击控件。
- [ ] 键盘焦点、Esc 关闭抽屉、Tab 顺序和屏幕阅读语义通过基础检查。

---

## 10. 阶段 E：终稿回传与 Design DNA 第二阶段（P1）

### 目标

让软件学习用户真正交付了什么，而不只是学习用户点了“采用”还是“拒绝”。

### 实现方法

1. 增加“设为终稿”和“上传修改后的终稿”：
   - 保存生成稿 asset、终稿 asset 与父子关系。
   - 记录对应 generation、Prompt、模型、知识来源和人工说明。
2. 建立差异证据：
   - 尺寸、裁切、主体位置、色彩、背景、文字、Logo、边缘和构图变化。
   - 自动分析只能提出证据候选，不能直接断言用户偏好。
3. 把反馈解析器从散落关键词升级为可维护的结构化模块：
   - 原始文本保留。
   - 解析出的 claim、维度、方向、强度和置信度可查看。
   - 每条候选显示支持证据和反例。
4. 完整作用域：`designer → brand → category → project → current task`。
5. 作用域优先级和冲突策略必须明确；品牌规则不能污染设计师全局，项目临时偏好不能自动升级为长期规则。
6. 已批准建议只能生成知识草稿：
   - 写入前检索去重。
   - 新页面或实质修改必须 `review: pending`。
   - 用户最终审核后才能成为正式规则。
   - 保存来源 suggestion ID、evidence IDs 和变更前后差异。

### 验收标准

- [ ] 用户可把某个生成结果设为终稿，或上传修改后的终稿。
- [ ] 从终稿可以反查源素材、任务、generation、Prompt、模型、知识来源和反馈。
- [ ] 同一偏好仅出现一次不会生成长期建议；达到证据阈值后才出现候选。
- [ ] 支持证据和反例均可在 UI 中逐条查看。
- [ ] 品牌 A 的规则不会影响品牌 B；项目规则不会无证据升级到全局。
- [ ] 正反证据持平时建议降权或撤回。
- [ ] 批准同一规则两次不会创建重复知识页。
- [ ] AI 生成的知识改动全部为 `review: pending`，无法自动改成 approved。
- [ ] 用户可以拒绝建议且保留拒绝原因与审计记录。

---

## 11. 阶段 F：模型路由与抠图质量升级（P1/P2）

### 目标

用可复现评测决定默认模型和抠图方案，而不是按模型名、宣传或单张效果主观更换。

### 实现方法

1. 建立 provider/model registry：
   - 能力：生成、编辑、多参考、透明图、最大尺寸。
   - 限制：输入数量、并发、超时、内容审核。
   - 成本、平均耗时、可用性和历史质量。
2. 建立至少 30 张有代表性的私有评测集：
   - 透明/反光包装。
   - 玻璃、金属、白色产品。
   - 毛发、细线、半透明边缘。
   - 包装文字和 Logo。
   - 合照与多主体。
   - 食品饮料真实主力类目。
3. 生成模型按主体保真、包装文字、Logo、数量、角度、构图、商业感、耗时和费用评分。
4. 抠图模型按主体完整、边缘、孔洞、半透明、阴影保留、污染和速度评分。
5. 新模型先作为可选实验引擎，达到门禁后才替换默认模型。
6. 路由原因写入 generation，例如“包装文字高风险，选择文字保真模型”。
7. 保留手动覆盖入口，并记录用户覆盖与最终结果用于后续路由学习。

### 验收标准

- [ ] 每个候选模型使用同一评测集、同一评分表、同一输出规格比较。
- [ ] 每个结果记录模型版本、参数、费用、耗时和失败原因。
- [ ] 新默认模型的加权质量分至少比当前基线提高 10%，且主体/文字/Logo 不出现 P0 退化。
- [ ] 抠图评测集中的明显主体缺失、严重毛边或透明区域破坏率低于既定发布阈值；阈值在首次基准测试后冻结。
- [ ] 模型不可用、额度不足、超时或审核失败时返回结构化错误并可选择替代引擎。
- [ ] 自动路由结果和理由在账本中可追溯。
- [ ] 真实云端评测开始前由用户确认预算；测试结束输出费用汇总。

---

## 12. 阶段 G：UI 减法、主题 DIY 与高级交互（P2）

### 目标

在功能语义和状态稳定后，把当前“信息偏满”的界面收敛为高级、克制、具有品牌辨识度的设计师产品。

### 实现方法

1. 与参考图并排拆解：整体构图比例、浮动左轨、深浅锚点、卡片节奏、留白、文字密度和焦点路径，不只模仿颜色。
2. 做文案减法：
   - 一屏只保留当前任务必须理解的信息。
   - 解释文本进入 tooltip、详情抽屉或首次引导。
   - 中文表达任务语义，英文用于短标签和氛围装饰。
3. 左侧导航融入整体画布，不使用突兀的额外边框；缩小 Logo，增加呼吸空间。
4. 右上红黄绿按钮按 macOS 交通灯视觉比例收敛，建议视觉直径约 12px，点击热区不小于 28px。
5. 主题设置采用语义 Token，不允许用户直接破坏所有 CSS：
   - 主色、强调色、背景、面板、文字、深色锚点。
   - 主模块衬底图/纹理。
   - 工作台标题、副标题和品牌短句。
   - 内置恢复默认和导入/导出主题。
6. 按钮和控件增加有意义的微交互：按压、加载、任务入队、完成、错误和知识命中反馈；避免无意义发光和过度动画。
7. 保持 Windows 原生窗口圆角与不透明边缘，不恢复假透明裁切方案。

### 验收标准

- [ ] 与参考图并排评审时，视觉层级、留白节奏和导航融合度达到统一设计语言，而不是局部换色。
- [ ] 1280×800 首屏无无意义长文案，核心操作路径可在 3 秒内识别。
- [ ] Logo、交通灯、标题和右侧面板比例不再产生拥挤感。
- [ ] 自定义主题可以保存、预览、恢复默认、导出和导入。
- [ ] 自定义极端颜色时仍保持可读性；关键文本对比度达到 WCAG AA。
- [ ] 浅色、深色、自定义三种主题在 960×600、1280×800、1920×1080 和 100%-175% DPI 下通过。
- [ ] 右侧控制区无滚动条，窗口边缘无底层内容透出。
- [ ] 所有关键按钮具备 hover/pressed/focus/loading/disabled/success/error 状态。
- [ ] 完成全状态截图：空白、已上传、排队、运行、部分成功、失败、取消、完成、抽屉、历史、成长中心、设置。

---

## 13. 阶段 H：发布、灾备与最终验收（P0-P3 汇总）

### 目标

交付可安装、可恢复、可迁移且不会泄露用户素材和密钥的正式版本。

### 实现方法

1. 建立自动测试层：
   - schema/migration 单元测试。
   - asset store 单元测试。
   - job 状态机与调度测试。
   - mocked 四工作流集成测试。
   - 前端关键状态与 API 合约测试。
   - Tauri/sidecar 冒烟测试。
2. 发布前创建全新用户目录测试首次启动和迁移启动。
3. 增加本地数据库与素材索引备份、恢复、导出说明。
4. 扫描 Git 历史和当前树中的密钥、数据库、用户素材和输出文件。
5. 生成 Release、NSIS、SHA-256、Git 标签和离线源码包。

### 最终发布门禁

- [ ] 所有自动测试通过，且测试环境未连接真实云端。
- [ ] 旧 schema v1 用户可以无损升级。
- [ ] 20 文件批次、取消、失败重试、部分导出、崩溃恢复全部通过。
- [ ] 四工作流素材语义正确，切模式不丢素材。
- [ ] 素材、任务、Prompt、模型、知识来源、反馈和终稿血缘完整。
- [ ] 真实云端只执行一组受控验收，并有费用记录。
- [ ] 1280×800、960×600、1920×1080 与 100%-175% DPI 通过。
- [ ] 安装、卸载、覆盖升级、数据库迁移、离线恢复均验证。
- [ ] API Key、用户配置、SQLite、素材、生成结果和模型缓存不进入 Git/安装日志。
- [ ] 新建回滚标签、离线 ZIP 和 SHA-256，并从 ZIP 解压后再次完成核心验证。
- [ ] 更新 `HANDOFF`、`ROLLBACK`、README 和知识库项目页。

---

## 14. 建议任务拆分与责任边界

### Codex 主责

- schema 与迁移框架。
- 素材存储与安全路径。
- job/job_items 状态机。
- 调度、并发、取消、重试和恢复。
- 前后端合约和主链路集成。
- Design DNA 作用域、终稿证据和知识写回安全。
- 模型评测框架、最终代码审查、Git 和发布。

### WorkBuddy 可分配

- 只读盘点某一模块的调用点和依赖。
- 按冻结的数据契约补测试用例草稿。
- 执行不联网黑盒测试并记录现象。
- 对照验收清单收集截图和日志。
- 更新说明文档、接口表和手工测试步骤。

WorkBuddy 的输出必须由 Codex 复核；不能让它自行扩展表结构、重写任务引擎或决定并发策略。

### 需要用户参与的节点

只有以下事项必须等待用户：

1. 使用真实 API 额度进行模型质量评测。
2. 提供/确认可以用于私有评测的代表性产品图。
3. 最终 UI 审美与主题方案确认。
4. Design DNA 建议和知识草稿的最终批准。

其余本地实现、测试、重构和安全确认由 Codex 自主完成。

---

## 15. 交付节奏与完成定义

### 第一可用里程碑：可靠素材与任务底座

包含阶段 A-D。达到后，软件应当真正支持：素材不丢、批量并发、后台运行、进度可见、取消和失败重试。

**未达到这组条件，不得声称“批量工作台完成”。**

### 第二可用里程碑：成长闭环

包含阶段 E。达到后，软件应当能从终稿差异和跨会话证据形成可审核的 Design DNA。

**未实现终稿血缘和作用域隔离，不得声称“软件会成长”。**

### 第三可用里程碑：设计师级正式版

包含阶段 F-H。达到后，默认模型有评测证据、UI 通过完整状态验收、安装和恢复链路稳定。

**未完成模型基准、全状态 UI、迁移和灾备，不得声称“达到应用级”。**

粗略工程量（以专注执行且不含等待用户素材/云端评测时间计算）：

| 里程碑 | 预计有效工程时间 |
|---|---:|
| 阶段 A-D：可靠底座 | 12-20 小时 |
| 阶段 E：成长闭环 | 8-16 小时 |
| 阶段 F：模型/抠图评测 | 8-16 小时，另需真实 API 运行时间 |
| 阶段 G-H：UI 精修与发布 | 12-24 小时 |

完整生产级目标不是“一晚全部完成”的任务。第一晚适合完成数据契约、迁移测试和素材存储主链；只有测试门禁通过后再进入队列与 UI。

---

## 16. 新账户可直接复制的启动提示词

```text
请接管 Product Atelier 的下一阶段开发。

正式仓库：D:\ProductAtelier-Desktop
GitHub：https://github.com/cat644142986-afk/-.git
可信基线：9b51da4e95339896726f1895c21ca0b816a5f68e
回滚标签：baseline-2026-08-21-pre-workspace-refactor

开始前必须完整阅读：
1. HANDOFF-NEXT-DEVELOPMENT-2026-08-21.md
2. HANDOFF-2026-08-21.md
3. ROLLBACK.md
4. 知识库项目页 PRJ-0001-ProductAtelier-AI商拍工作台.md

当前第一目标不是继续做 UI，而是按交接文档阶段 A-D 完成：
数据库显式迁移 → 素材内容寻址持久化 → jobs/job_items 状态机 → 有界并发/取消/重试/崩溃恢复 → 素材工作台和实时任务 UI。

必须遵守：
- 不新建第二数据库；
- 不把源图放进 output；
- 不用 daemon 线程代替任务系统；
- 不让切换模式清空素材；
- 不新增 vXX 补丁层；
- 不联网消耗生图额度；
- 不自动批准或写入正式知识；
- 不提交 API Key、配置、数据库、素材和输出。

先检查 Git 状态和现有实现，逐条对照交接文档给出本轮将完成的阶段、文件、测试和风险；确认代码事实后直接开始阶段 A，不要重新设计 UI。每完成一个阶段必须运行该阶段验收门禁，失败就修复，不得跳过。
```

---

## 17. 回滚

任何核心改造失败时，不要 `git reset --hard` 覆盖工作区。按 `ROLLBACK.md` 从标签创建独立恢复分支：

```powershell
git switch -c restore/baseline-2026-08-21 baseline-2026-08-21-pre-workspace-refactor
```

当前离线基线还保存在：

- `D:\ProductAtelier-Backups\ProductAtelier-baseline-2026-08-21-pre-workspace-refactor.zip`
- `D:\ProductAtelier-Backups\ProductAtelier-baseline-2026-08-21-pre-workspace-refactor.sha256.txt`
- `D:\ProductAtelier-Backups\restore-audit-9b51da4`

回滚后仍需重新运行 Vite、Python、memory smoke test 和 Rust 验证，不能只看应用能否打开。

