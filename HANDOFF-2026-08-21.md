# Product Atelier 继续开发交接文档

> **下一阶段执行请优先阅读：`HANDOFF-NEXT-DEVELOPMENT-2026-08-21.md`。** 该文档补充了持久素材、可靠并发任务、终稿回传、模型评测和 UI 精修的分阶段实现方法与验收门禁；本文继续作为 Growth Studio 已完成基线记录。

> 更新日期：2026-08-21
> 当前基线：Growth Studio 第一阶段已落地并完成正式版验收。产品不再按“图片处理工具”定义，而是以可追溯、可学习、需用户审核的 AI 设计伙伴为核心。

## 1. 产品北极星

Product Atelier 的目标不是帮设计师少点几个按钮，而是让软件在长期使用中理解其设计判断：

1. 生成前：知识库参与 Creative Brief、Intent Locks 与 Prompt 编译。
2. 生成中：保存素材血缘、模型、Prompt、知识来源和任务状态。
3. 生成后：记录采用、拒绝和调整原因，形成可审核的成长证据。
4. 长期：重复且有证据的偏好进入 Design DNA 建议；未经用户确认不得写成正式知识。

北极星指标优先看终稿采用率、平均修改轮次、可复用偏好命中率和知识来源可追溯率，不以按钮数量或页面数量衡量完成度。

## 2. 本轮产品与交互决策

- 左上 Logo 永久只作为品牌标识；旧版贴边、停靠、迷你工具栏路径已从前端和 Rust 命令层删除。
- 右上保留红/黄/绿窗口按钮，用于关闭、最小化、最大化/还原。
- 原生窗口使用 Windows 11 系统圆角和阴影，WebView 保持不透明，避免圆角边缘透出桌面或底层窗口。
- 右侧任务控制区固定在一屏内且 `overflow: hidden`，不使用滚动条；复杂参数进入独立抽屉。
- 采用暖米色、深色锚点、珊瑚橙和柔和黄组成的高端 Bento Studio 视觉系统。
- 中文承载任务语义，英文用于工作流、状态和氛围标签，不做机械式全中文。
- 单产品、多文件、合照拆分、批量抠图是四种独立工作流，不再共用错误的上传语义。

## 3. 当前运行时架构

### 前端

- `src/index.html`：Studio、对比、创作会话、成长中心、设置和两个参数抽屉。
- `src/css/stable-ui.css`：唯一生产样式与语义 Token，含浅/深主题、响应式和 `prefers-reduced-motion`。
- `src/js/app.js`：页面、工作流、素材队列、知识预编译、结果、反馈和窗口交互状态。
- `src/js/api.js`：Tauri、FastAPI、文件系统、知识、账本和成长 API 适配层。

旧 `vXX-*.css/js` 文件仅保留追溯，不允许重新接入生产入口。

### 本地后端

- `python/server.py`：FastAPI 主服务和四种工作流入口。
- `python/atelier_ledger.py`：SQLite 创作账本，记录 sessions、assets、generations、events、feedback、memory_suggestions。
- `python/knowledge_engine.py`：只读 Markdown 知识编译器，保留来源路径、规则、冲突和 Intent Lock 优先级。
- 正式数据库：`%APPDATA%\ProductAtelier\atelier.sqlite3`。
- 默认知识库：`C:\Users\64414\Documents\知识库`，当前只读扫描 `20 知识库\设计知识`。

开发版 Rust 现在优先运行源码 `python/server.py`，避免 `target/debug` 中旧 sidecar 导致“浏览器开发版可用、桌面版接口缺失”。发布版使用 PyInstaller onedir sidecar。

## 4. 已实现能力

### 四种工作流

1. `single`：单张可信源图，生成商业主图和透明 PNG。
2. `multi-file`：最多 12 张独立源图、最多 24 个计划输出；逐文件记录成功、失败和素材血缘。
3. `group-split`：一张包含多个产品的合照，识别主体后分别输出，不与多文件批量混淆。
4. `cutout-batch`：最多 24 张本地批量抠图，不调用云端生成额度。

对应接口：`/api/single`、`/api/multi-file`、`/api/group-split`、`/api/cutout-batch`。旧 `/api/multi` 暂保留兼容别名。

### 知识与成长闭环

- 当前索引：61 份设计知识文档、2094 条候选规则。
- 编译输出：Creative Brief、6 类 Intent Locks、正向规则、负面规则、来源和冲突说明。
- Intent Locks：主体外形、包装文字、品牌色、Logo、角度、产品数量。
- 每次创作会话可保存 brief、知识来源、生成记录、反馈和事件时间线。
- UI 已提供“采用这版”“需要调整”和原因输入，并接入本地反馈 API。
- Design DNA 证据聚合第一版已接入：一个会话的单次反馈不会形成偏好；至少 2 个独立会话重复同类判断才生成候选，未知自由文本需 3 个独立会话。
- 候选保存支持证据、置信度、反例和作用域；正反证据持平会自动撤回，用户批准后同值建议不会重复出现。成长中心负责展示并接受/拒绝候选，仍不会自动写入正式知识库。

## 5. 已完成验证

| 验证项 | 结果 |
|---|---|
| Python `py_compile` | 通过 |
| Vite 生产构建 | 通过 |
| Rust `cargo check` | 通过 |
| Tauri Release / NSIS | 通过 |
| 1280×800 原生窗口 | 通过；窗口边缘不透明、任务区无滚动条 |
| 960×600 浏览器视口 | 通过；任务区 `scrollHeight == clientHeight` |
| 正式版 sidecar | 通过；账本 schema v1，知识 61 docs / 2094 rules |
| 正式版四工作流路由 | 全部存在 |
| 知识编译 | 通过；10 条正向、14 条负向、10 份来源、Intent Locks 生效 |
| 单产品 mocked 生成 | 通过；会话、generation、2 个资产和来源可追溯 |
| 多文件 mocked 生成 | 通过；2 源图形成 2 个独立 generation |
| 批量抠图 mocked 生成 | 通过；逐文件结果和状态保存 |
| Design DNA 证据聚合 | 通过；单次不提案、两会话提案、反例降权、持平撤回、批准后不重复 |
| 无障碍基础项 | 焦点可见、弹层语义、键盘对比滑杆、减弱动效已补齐 |

真实云端生图本轮未运行，避免无必要地消耗用户额度。正式版 API Key 仍沿用本机已有配置。

## 6. 构建产物

- Release EXE：`D:\ProductAtelier-Desktop\src-tauri\target\release\product-atelier.exe`
- NSIS 安装包：`D:\ProductAtelier-Desktop\src-tauri\target\release\bundle\nsis\Product Atelier_1.0.0_x64-setup.exe`
- 安装包大小：129,865,774 字节
- SHA-256：`E1E44ECE5704A4F4223CD4D1E7FE5A93237E7BA6533C7BCFEEF2F540376EC25B`
- 桌面快捷方式：`C:\Users\64414\Desktop\Product Atelier（新版）.lnk`

Tauri 仍提示 identifier `com.productatelier.app` 以 `.app` 结尾；它不影响 Windows，但修改会影响应用身份和升级路径，因此本轮未改。

## 7. 下一阶段顺序

### P0：Design DNA 第二阶段

1. 将当前关键词归类扩展为可维护的反馈解析器，并显示每条原始证据与反例详情。
2. 接入“设为终稿”和终稿回传，让生成稿与终稿差异成为比按钮点击更强的证据。
3. 为已批准候选生成正式知识页草稿；写回前去重，页面必须 `review: pending`，且由用户最终确认。
4. 增加作用域迁移：设计师全局、品牌、类目与项目之间不得互相污染。

### P1：生成可靠性与错误设计

1. 统一 Rust 与 Python 配置真相源。
2. 接入可查看的 sidecar 日志、崩溃检测和一次自动恢复。
3. 为额度不足、模型不可用、超时、内容审核和局部失败建立结构化错误码。
4. 多文件队列支持暂停、重试失败项、跳过和部分导出。

### P2：设计师级评审

1. 增加轮廓、包装文字、Logo、品牌色、数量和角度的自动检查结果。
2. 对比页增加差异标注、批注和“设为终稿”。
3. 终稿与生成稿的差异进入学习证据，而不是只记录按钮点击。

### P3：产品化与自动回归

1. 固化状态矩阵和四工作流冒烟测试。
2. 验证 125%、150%、175% DPI 与多显示器。
3. 增加本地数据库备份、导出、迁移和隐私控制。

## 8. 开发纪律

1. 禁止新增 `vXX-fixes.css`、补丁脚本、MutationObserver 强制布局。
2. 页面显隐只由 `switchPage()` 管理，画布阶段只由 `setStage()` 管理。
3. 新功能必须先定义它要解决的设计任务、输入语义、输出和学习证据，再实现界面。
4. 知识库只读介入生成；任何正式知识写回都必须可追溯、去重并等待用户审核。
5. 每次发布至少验证：构建、sidecar 版本、知识索引、四工作流路由、1280×800、960×600、浅/深主题、键盘焦点。
6. 云端生成测试必须记录模型、输入、耗时、费用和输出，禁止静默消耗额度。

## 9. 回滚与追溯

- Growth kernel 改造前：`D:\ProductAtelier-Desktop\backups\20260821-150045-pre-growth-kernel`
- 布局重建前：`D:\ProductAtelier-Desktop\backups\20260821-131735-pre-layout-rebuild`
- 稳定化备份：`D:\ProductAtelier-Desktop\backups\20260821-pre-stabilization`

工作区在本轮开始前已有大量用户修改和历史文件。本轮没有执行 reset、checkout、清理或提交；比较历史时优先使用备份和 `git diff`。
